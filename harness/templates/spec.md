# 需求规格：{{功能名}}

> 作者：Planner | 日期：{{date}} | 项目：{{project_name}}

## 概述
{{简要描述}}

---

## 功能需求

### P0（必须有）
| ID | 需求 | 验收标准 | 关联 API |
|----|------|---------|---------|
| FR-1 | {{需求描述}} | {{可衡量的标准}} | {{接口}} |

### P1（应该有）
| ID | 需求 | 验收标准 | 关联 API |
|----|------|---------|---------|

### P2（锦上添花）
| ID | 需求 | 验收标准 | 关联 API |
|----|------|---------|---------|

---

## API 契约

### 接口：{{METHOD}} {{path}}
- **描述**：{{做什么}}
- **认证**：{{无 / Bearer token / API key / ...}}
- **请求**：
  - 路径参数：{{无 / ...}}
  - 查询参数：{{无 / ...}}
  - 请求体（JSON）：
    ```json
    { "field": "类型 // 描述" }
    ```
- **响应 200**（JSON）：
    ```json
    { "field": "类型 // 描述" }
    ```
- **响应 400**：{{条件}}
- **响应 401**：{{条件}}
- **响应 404**：{{条件}}
- **响应 500**：{{条件}}

<!-- 每个接口重复 -->

---

## 数据模型

### 新增：{{实体名}}
| 字段 | 类型 | 约束 | 描述 |
|------|------|------|------|

### 修改：{{实体名}}
| 字段 | 变更 | 原因 |
|------|------|------|

---

## 文件清单

### 创建
| 文件 | 层级 | 负责人 | 描述 |
|------|------|--------|------|

### 修改
| 文件 | 层级 | 负责人 | 变更说明 |
|------|------|--------|---------|

---

## 非功能性需求

### 性能
- {{指标 — 例如 "API p95 延迟 ≤ 200ms"}}

### 安全
- {{要求 — 例如 "所有接口需要 JWT 认证"}}

### 可靠性
- {{要求 — 例如 "数据库不可用时优雅降级"}}
